# HANDOFF — read this first

**State (2026-08-17 ~01:30 HKT):** the owner ruled: stop patching the legacy
pipeline; keep the peak frozen; rebuild clean. Read **REBUILD.md** — it is
the constitution: branch map, champion/challenger law, delivery gate,
architecture spec, validation protocol.

- Champion (frozen, showable): branch **stable-run105** — 80.6% / balance /
  keys 11-14 / 38 min. Its actual output workbook and the hand-verified
  deliverable are in `companies/Dongfang Electric/model/` (Project M tree).
- New main: branch **rebuild** — new code ONLY in `pipeline/`; `agent/` is
  read-only legacy reference (validated prototypes: join.py, fablemode.py
  sign-rule, workbook.py guards, posteval.py; disease history: RUNLOG.md,
  tests/test_museum.py).
- Presentation materials: PRESENTATION.md (current), council/ transcripts.
- Next session: start with REBUILD.md, build Stage 1-2 in pipeline/ against
  pinned snapshots, museum green, then the first challenger run — and only
  claim victory if it beats the champion on the owner's four criteria plus
  the delivery gate.

History and every autopsy: RUNLOG.md. Owner's standing directives: pure
gpt-5.6-luna in runs, broadly-available Python only, generic across
companies, dry-test before every dispatch, council on major decisions.
