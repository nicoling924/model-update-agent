Vendored toolchain (not our code): typescript.js 5.5.4 + its lib/*.d.ts
(unpkg), excelscript.d.ts (OfficeDev/office-scripts-docs-reference,
api-extractor-inputs-excelscript). Used by typecheck.jxa/transpile.jxa
so the bench sees exactly what the Office Scripts editor sees.
