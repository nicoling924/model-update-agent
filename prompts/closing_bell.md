# CLOSING BELL — one terminal disposition for this residual generator

The close is ending. This check family is still failing — the residual
vector below is ONE generator (a reported-year cause propagates into
every forecast year; one disposition closes the vector).

How a close ends, in the analyst's own method: back out the number and
mark it. A flagged plug on a non-key line lets the analyst review a named
orange cell in seconds; a broken identity poisons every downstream year.
Balanced-with-a-confession beats broken-with-a-note; both beat fiction.
A flag is still legal — when no legal plug line exists, or plugging would
move a disclosed value (truth outranks balance) — but it must carry the
reason the plug was unavailable, in words the analyst can act on.

Decide NOW, one JSON object:
{"disposition": "plug", "into": "Sheet!U177", "why": "<what this residual
  is, why this line absorbs it, cited if possible>"}
or
{"disposition": "flag", "why": "<why no legal plug exists — the analyst
  reads this>"}
