# Audits and diagnosis of runs 35066977462 / 35067022855 (head dc21460)


## AUDIT — does the ending enforce the objectives

**Verdict up front: this is a loop that PROPOSES fixes and exits on the clock. Nothing in it can guarantee objective 1.**

**(a) Exit paths — all of them leave breaks open.** `run_ending` (consequence.py:709-856) has six exits, none conditioned on the objectives holding:
1. **Clock** — `elapsed > deadline_s` (consequence.py:713-718). **This fired: r1354**, 9 of 14 rounds used.
2. **`_breaks()` empty** (:720-722) — but `_breaks` filters out anything in `judged` (:660), so "empty" means *judged*, not *fixed*.
3. **`question`** → `judged.add(akey)`, continue (:791-798). Fired 6×: r1188, r1241, r1294, r1297, r1300, r1353.
4. **No answer from brain** → judged; only here do `check`/`forecast-check` enter `unanswered_checks` → last resort (:774-783). Did not fire.
5. **Take-back with no untried way** → judged (:844-846). Fired after r1238/r1291/r1350.
6. **Inherited/dedupe filters** (:627-634) — ROAFNA!r31 2024 dropped; all six forecast years of Final!r99 collapse to one objective.
Then run.py:1487-1490 saves and logs `DELIVERED WITH OPEN CHECKS` unconditionally — `ok` gates nothing.

**(b) Budget: 9 rounds, 6 objectives, zero net writes.** r1186 check ROAFNA!AI31→question; r1189 Final!AI99→derive:1→**taken back r1238**; r1239 same break→question; r1242 key Final!AI96→derive:1→**taken back r1291**; r1292 same→question; r1295 key Final!AI15→question; r1298 sanity Final!AK57→question; r1301 Final!AM99→derive:2→**taken back r1350**; r1351 same→question. Three of nine rounds are re-deals of a break judged the round before (the `tried` retry, :758-761), each paying a fresh census + card. Ranking `{check:0,key:1,sanity:2,forecast-check:3,sense:4}` (:598) gave objective 3 (cash) *one* round, seventh, and the sense lines **zero** — the clock hit first. The 22 collapsed forecast rows (r1181, r1185, r1211…) are logged 20+ times and never become an objective at all.

**(c) The card ranks by share of the value move, not by effect on the objective — and it is wrong by a factor of 13.** Cash card r1299 listed HK Sales!AI16 at **8%** of the move (India!AI5 8%, CN!AI160 6%). Measured on the delivered workbook (restore each mover's pre-run content, re-read `Final!AK57`, base −20,153):
- **HK Sales!AI16 → +600. It alone explains 103% of the break** (card said 8%).
- India!AI5 → −19,564 (3%; card 8%). CN!AI160 → −17,790 (12%; card 6%). All three → +3,551.
The one true culprit *was* on the card, labelled as explaining almost none of it. The card also offered a false lead — "20,092 ≈ Gas (p39)" (`name_gap`, :503) — a coincidental magnitude match. On the Final!AI99 card the same census printed "**1916% of the move**" (r1190) — the census knows the movers offset, and prints a nonsense percentage instead of naming the pair. Worse, previews (:459-475), the only place where consequence is actually computed, are silently dropped when `left_s <= 120` (:755) and **no line on the card says they are missing** — the brain cannot tell a card with measured consequences from one without.

**(d) Compensating pairs are structurally unresolvable here.** Delivered Final!AI95 RE = 88,242 vs key 84,367 (**+3,875**); AI97 MI = 5,943 vs 9,815 (**−3,872**); net check +223. `apply_pick` writes **one** mover (:266-310) and the verifier rejects any pick that raises total mass (:833-840). Fixing either half alone moves the check from +223 to ±3,6xx, so mass rises 35,459→57,173 and it is reverted — exactly r1238, r1291, r1350, each "opened [('Final','AI98')]". The loop then marks the break judged and never returns. The only pair-aware construct in the repo is `terminal_ladder`'s PAIRED DIFF (orchestrator.py:2062-2100), which sits inside the ladder that never ran.

**(e) Why no plug — and no guarantee.** Pre-ending PLUG cards were refused by the brain (r1129, r1131, `refuse_flag`, workqueue.py:1200). That card's own text promises "leaves the check failing, loudly, **for the terminal ladder**" (workqueue.py:1192-1194) — a promise the ending breaks: the ladder is reached only via `unanswered_checks`, i.e. only when the brain says *nothing* (:780-783). The brain said "question" (r1241), so `judged` swallowed it. Forecast plugs were separately withheld because the actual year was off (forecast_balance.py:167-173; r1182-83, r1235-36). `terminal_ladder` is no longer called from run.py at all. Net: **0 plugs (r1366)**. The design guarantees objective 1 only for a brain that is silent or says "plug"; a brain that says "question" disarms the last resort entirely.

**(f) Minimal structural changes, ranked by impact (no rules, all evidence/state):**
1. **Last resort on state, not words** — after the loop, any actual-year check still ≠ 0 goes to `terminal_ladder` regardless of what was answered; "question" flags but does not disarm. *Guarantees objective 1.* ~2-3 h.
2. **Movers ranked by consequence, previewed by code** — for each mover compute the objective under restore and rank/print *that*; never suppress previews near the clock (compute the top 3 only). Would have put "restores cash to +600" next to AI16 at r1299. *Objectives 3 and 2.* ~4-6 h.
3. **Pairs as one pick** — when censused movers offset (the >100% signal already present), offer the pair as a single transactional option and measure it as one. *Objective 2 (the 3,875/3,872 pair).* ~5-8 h.
4. **Exit only when every objective holds or each break carries a named, verified reason** — `judged` must store a reason object the gate re-verifies, and the clock reports "unreasoned breaks: N" rather than ending silently. ~3 h.
5. **Sense/collapse lines re-measured after every write and admitted as objectives** (the 22 collapsed rows). ~3 h.

## AUDIT — does the sense check work

**AUDIT OF THE SENSE CHECK — CLP FY25 run 35066977462 (dc21460)**

**(1) When it runs, what it sees, what it can do**

Two call sites only.

- `pipeline/run.py:1151-1153` — `checkpoint(loop, _pre_wb_sense, log)`, after the automatic fill, **before** `run_queue` (run.py:1160). This is r602-661.
- `pipeline/consequence.py:639` — inside `run_ending._breaks()`, `suspicious(headline_deltas(...))` appended each round.

Nothing re-measures in between. `grep -n sensecheck pipeline/workqueue.py pipeline/investigate.py pipeline/orchestrator.py` → **empty**. The card queue (r662-1180, incl. the serve at r796) never touches it.

What it looks at: `sense_rows` (sensecheck.py:40-67) = P&L + Cash-flow roles of the report table only — 18 lines here, all on `Final` + `Aus!23`. Per line it compares FY0 and FY+1 vs the pre-update book (`headline_deltas`, sensecheck.py:70) and trips at >10 points (`SENSE_GAP`, :31). Cells only enter via `swing_leaves` census inside `investigate_line` (:249), **top 4 by share of swing**.

What it can do: a RUNG card (keep / printed / estimate / lastyear / derive:via). It cannot revert a serve — at r607 the brain's `printed` pick was refused by the evidence law and the verdict was just "please look here".

After the ending's picks, `_finish` (consequence.py:643-670) re-measures and writes closing `sense_rows`, but that is **report text only** — no card, no fix.

**(2) Would it have caught it? Yes, first on every line.**

Reproduced offline (`discover` on the pre-archive, delivered workbook):

| line | gap | census rank 1 |
|---|---|---|
| revenue `Final!AJ7` | 17.4 pts | **HK Sales!AI16 +67%** |
| operating cash flow `Final!AJ121` | 82.0 pts | **HK Sales!AI16 +35%** |
| free cash flow `Final!AJ145` | 30.6 pts | **HK Sales!AI16 +49%** |

AI16 is rank 0 of 7-8 leaves on all three. At the checkpoint, `revenue`/`ebitda`/`EBIT`/`pre-tax` were **not** suspicious (log r602-685); after the write all four are (17-72 pts). The write manufactured four new suspicious lines that were never re-measured into a card.

True effect (restore `HK Sales!AI16 = 44.3`, `Evaluator`): `Final!AJ57` −7,914 → **+1,403**; `AK57` −20,153 → **+600**; `AM57` −46,153 → **−637**; `Final!AJ7` 73,884 → **87,453**; OCF `AJ121` 9,396 → **19,736**; FCF `AJ145` −3,116 → **+7,465**. The 8%/13532% shares on the r1299/r1302 cards understate it because they are shares of one broken *check*, not of the objective.

**(3) The history signal never fires as a trigger.** `unusual_by_history` (investigate.py:350) is called only at investigate.py:411, *inside* `judge_and_fix` — i.e. only for a cell the census already selected. It is card prose (investigate.py:659), never an entry point. The magnitude sweep (gate.py:87, `BAND_RATIO = 100.0`, :31) and the writer's world band (writer.py:49) refuse >100× or <1%: 1.00/44.30 = 2.26% — inside the fence by a factor of two. `tripwires` = `sign_absurd_rows` only (run.py:1123); sign unchanged. So nothing looked at 30.2 / 46.1 / 58.4 / 44.3 → 1.00 at write time.

**(4) The card (r796) lied about the evidence.** Option B's badge comes from workqueue.py:283-284: the candidate is built with a hardcoded `"tie_off": 0.5`, and the renderer (workqueue.py:935-937) prints "✔ prior tie EXACT (this line's comparative = the model's prior)" for any `tie_off ≤ 0.6`. B has **no comparative and no number tie** — 0.5 is a placeholder for a *label* match in last year's report. The tick itself is stored in `warnings` and rendered `⚠ ✔ …` (workqueue.py:929) — positive wording in the warning channel, and it *reduces* A's apparent doubt (A carried 2 real ⚠, B 1).

AR p243 (pdfplumber) reads: `Fuel Cost Adjustment 1 46.3 46.3 62.0 38.6 28.1`. **1.00 is the footnote superscript**, not a figure of the row. The row's 2025 value is 46.3 — the analyst's key.

**(5) Verdict: a one-time early look, not a safeguard.** It is measured once against a pre-update book, on 18 headline lines, before the queue writes anything; at the ending it is objective kind `"sense"` = rank 4 of 4 (consequence.py:599), and the ending exited on the clock at round 9 with 12 breaks open (r1354) having never reached one. Objective 3 was unguarded from r662 onward.

Minimal structural changes (no rule about what to pick):

1. **Re-measure the sense lines after any write that reaches one of their inputs** — `_leaves` of each `ref1` is already computed; intersect it with the writer's write log and re-run `headline_deltas` on the touched lines, queueing a RUNG card when a line crosses `SENSE_GAP` that did not before. **~6-10 h.**
2. **A cell's move against its own history is a card on its own** — `unusual_by_history` promoted from card prose to a queue producer at write time, independent of any census. **~4-6 h.**
3. **Rank the ending's breaks by consequence on the objective, not by kind** — drop the `order` dict tiering so a 82-point sense gap competes with a 109 check. **~3-5 h.**

Separately, and cheapest: workqueue.py:283 should not synthesise `tie_off`; a noun-proven candidate with no number tie must render as such (**~1-2 h**).

## DIAGNOSIS — runs 35066977462 (CLP) and 35067022855 (DFE)

**CLP run 35066977462**

**(1) Unbalanced, 0 plugs.** The last resort is gated on the brain's *word*, not the model's state: `run_ending` fills `unanswered_checks` only when the pick isn't an option (`consequence.py:776`) and calls `terminal_ladder` only for those (`consequence.py:866–874`). `question` is `judged` + `continue` (`consequence.py:791–797`); the queue's PLUG card default `refuse_flag` (`workqueue.py:1200`) likewise ends the check. Tonight both PLUG cards refused (r1129, r1131) and every check answered question/derive (r1188, r1241, r1294) → ladder never ran. Run 35043265913 balanced only because the brain happened to say `plug` (r2.log:1309 → :1311). Tonight's 0b1908e / cf9f4b2 / 7aa139d did **not** remove the path — it was never reachable except by wording.

**(2) The identical 35,459→57,173.** Real, not a measurement bug. All three picks resolved to the same write: `apply_pick` derive:N → `t_derive(movers[0] = Final!AI95)` → 84,367 (writes.json i=611/672/731). Reproduced: setting AI95=84,367 moves the balance mass (Final!99 + ROAFNA!31 over six year columns) 1,787 → 23,501, Δ = **+21,714 = 57,173 − 35,459 exactly**. Cause: AI95's +3,875 is *cancelling* a −3,872 understatement in MI (Final!AI97 5,943 vs analyst 9,815 = perpetual capital securities). Fix one and the check opens 3,875 in all six columns. The loop judges one pick at a time (`consequence.py:836`), so a compensating pair is inescapable.

**(3) Where +3,875 came from.** r704: SERVE Final!95 correctly answered derive:1 → AI95 = 84,367 "so that Final!AI96 equals its proven 107,610". Then **r945–946**: COMPONENT Final!99 offered fix:0 Final!AI97 5,943→9,788 (AR p237 'Other non-controlling interests', residual −3,652→193) and fix:1 Final!AI95 84,367→**88,242** (AR p211 'Balance at', residual −3,652→223). Brain took fix:1 → writes.json i=534 (unlogged; note now "Updated per the disclosure (annual report p211)"). The card ranks by residual+warnings only (`workqueue.py:1140–1152`) and the write gate never consults the key panel (no `keys_before` in `writegate.py`), so nothing warned that fix:1 breaks two proven keys. MI stayed 5,943 because fix:0 was simply not picked; the gate saw it only afterwards (r1375).

**(4) Forecast cash collapse: one cell.** HK Sales!AI16 'Fuel Clause Charge/(Rebate)' 44.3 → **1.00** (writes.json i=492) from r796 option B, AR p243 'Fuel Cost Adjustment' — whose own warning reads "LAST YEAR'S report prints the model's prior under this label" (r797). Analyst holds 46.3. Restoring it alone: Final!57 2026 −7,914→+1,403, 2027 −20,153→+600, sanity mass 28,067→0 — 79% of the whole objective mass and every watch at r1355–1357.

**(5) vs run 35043265913.** Better: op profit 13,812 (was 11,957) = analyst, other income 460, EPS/NP tie; unflagged-wrong inputs 15→9. Worse: delivered unbalanced (Final!99 +223/+109×5, ROAFNA!31 −1,019), equity/L+E/RE keys broken (were all correct), HK Sales!AI16 46.3→1.0 so 2030 cash −10,175→−59,050, input accuracy 61%→59%.

**DFE run 35067022855.** 87% vs Fable 5 (201/231) — better than run 19 (78%). All four check rows (`Model!U95/U135`, `Driver!J109/J139`) read 0 every year, but 2025 passes falsely: `Raw financials!U243` holds `=(18981)-(-10087)` — a consequence back-out (`consequence.py:305`, r728-738). Keys 6/11 vs 8/11: `investing cash flow` (Model!U120) and `cash year end` (Model!U133), both from `Raw financials!U203` = 35,262 (2024) vs printed 25,155.7 — the reader refused the row at r389 (`reader.py:527`), so ROLLOVER r632 offered no printed option and the brain reverted. Also worse than run 19: `U207` = 0 vs 19.08; 45 red vs 14.

**Minimal root-cause fixes, in priority order**
1. A candidate whose comparative ties only in *last year's* report is not this year's figure — refuse it as a serve (HK Sales!AI16; DFE U203 is the mirror image).
2. Any card offering a way to close a check must show each way's effect on the proven key panel; the write gate must refuse a write that moves a rule-2 key off its print.
3. A value this run derived to close a proven key is a proven holder — `holder_is_proven` must lock it (a derivation lands conf 3, `orchestrator.py:1683`).
4. The last resort must fire on the model's *state* at exit — any check still off goes to `terminal_ladder`, whatever the brain answered.
5. The ending must be able to apply an offsetting pair as one pick, or `check_mass` will keep taking back every correct half.

**Better deliverable: run 34993405014, clearly.** Both check rows 0 in every year, total equity 107,610, L+E 238,644, RE 84,367 all tie the print, cash only −966 (2026) then positive. Tonight's file is better only on the P&L (op profit 13,812 vs 11,957). Ship 34993405014; carry tonight's P&L cells across.